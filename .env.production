# Production build (npm run build) — overrides .env.
# Lead forms post to the same domain (https://focasedu.com/api/...), which nginx
# proxies to focas-lead-server. Never point this at localhost for a live build.
VITE_COUNSELING_API=
