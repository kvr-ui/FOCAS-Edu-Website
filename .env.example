VITE_RAZORPAY_KEY_ID=rzp_test_SYBepV88dkOJSb
VITE_BACKEND_URL=http://localhost:7000
VITE_RTI_BACKEND_URL=http://localhost:8000

#VITE_BACKEND_URL=https://api.focasedu.com
#VITE_RTI_BACKEND_URL=https://backend.focasedu.com

# ── Counseling form → Bigin (server/index.js) ──
# Zoho OAuth creds (server-side only; never exposed to the browser)
ZOHO_CLIENT_ID=1000.xxxxxxxxxxxxxxxxxxxx
ZOHO_CLIENT_SECRET=your_client_secret
ZOHO_REFRESH_TOKEN=1000.xxxxxxxx.xxxxxxxx
ZOHO_REGION=in
COUNSELING_PORT=7001
COUNSELING_ALLOWED_ORIGINS=http://localhost:8080,https://focasedu.com,https://www.focasedu.com
# Frontend → where the counseling form POSTs (set to the deployed server URL in prod)
VITE_COUNSELING_API=http://localhost:7001
# External leads API — receives full student details + UTM params (never stored in Bigin)
LEADS_API_URL=https://your-leads-api.example.com/api/leads/web
# Rate limit for the counseling endpoint (per IP)
COUNSELING_RATE_MAX=5
COUNSELING_RATE_WINDOW_MS=600000
COUNSELING_TRUST_PROXY=true

