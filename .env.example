VITE_RAZORPAY_KEY_ID=rzp_test_SYBepV88dkOJSb
VITE_BACKEND_URL=http://localhost:7000
VITE_RTI_BACKEND_URL=http://localhost:8000

#VITE_BACKEND_URL=https://api.focasedu.com
#VITE_RTI_BACKEND_URL=https://backend.focasedu.com

# Lead forms (counseling / workout-batch) API base.
# Blank = same origin (the focas-lead-server serves the site + API on one port).
# Set only if the API runs on a different host/port.
VITE_COUNSELING_API=

# NOTE: Zoho/Bigin credentials + leads-API config moved to the separate
# "focas-lead-server" project (see its .env.example).
